/** @type {import('next').NextConfig} */
const nextConfig = {
    typescript: {
      // Setting this to true will ignore typescript errors on build time
      ignoreBuildErrors: true,
    },
    // ...other configurations
  };
  
  export default nextConfig;
  